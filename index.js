var NumberEncoder = require("./lib/NumberEncoder.js");
var NumberDecoder = require("./lib/NumberDecoder.js");

module.exports = {
	NumberEncoder : encoding.NumberEncoder,
	NumberDecoder : encoding.NumberDecoder,
};
